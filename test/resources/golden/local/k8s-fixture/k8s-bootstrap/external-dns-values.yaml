provider:
  name: cloudflare
policy: "upsert-only"
registry: txt
txtOwnerId: "k8s-fixture"
domainFilters:
  - "fixture.example"
env:
  - name: CF_API_TOKEN
    valueFrom:
      secretKeyRef:
        name: cloudflare-api-token
        key: api-token
extraArgs:
  cloudflare-proxied: "false"
