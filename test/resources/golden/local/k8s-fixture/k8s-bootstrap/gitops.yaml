apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: colors
  namespace: flux-system
spec:
  interval: 1m
  url: "https://github.com/getcolors/k8s-hetzner.git"
  ref:
    branch: "main"
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: colors
  namespace: flux-system
spec:
  interval: 1m
  retryInterval: 30s
  timeout: 10m
  path: "./gitops"
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: colors
